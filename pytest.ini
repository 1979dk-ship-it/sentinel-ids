[pytest]
# Put the project root (sentinel/) on sys.path so tests can import the app
# packages the same way main.py does: `from core...`, `from db...`.
pythonpath = .

# Only look for tests here, so a bare `pytest` never wanders into other dirs.
testpaths = tests
